 built a flight management system using PHP and MySQL. It has five pages: index.php (view flights), create.php (add flight), update.php (edit flight), delete.php (remove flight), and db.php (database connection).  used Bootstrap 5 for styling. The database name is airport_db with a flights table. This project performs all CRUD operations.
